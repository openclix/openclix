# OpenClix Retention Ops (Claude Code)

Project root: `{{ROOT}}`
Delivery mode: `{{DELIVERY_MODE}}`
Selected analytics provider: `{{SELECTED_PROVIDER}}`
Dry run: `{{DRY_RUN}}`

## Inputs
- Impact metrics: `{{IMPACT_FILE}}`
- Campaign metrics: `{{CAMPAIGN_METRICS_FILE}}`
- Active config: `{{CONFIG_FILE}}`
- Evaluator recommendations: `{{RECOMMENDATIONS_FILE}}`
- Evaluator next config: `{{NEXT_CONFIG_FILE}}`

## Task
1. Review retention/engagement deltas and evaluator proposals.
2. Validate recommendation quality against conservative sampling rules.
3. Suggest copy/timing/suppression updates only where confidence is high.
4. Produce a release-ready operations brief for weekly or biweekly review.

## Output format
Return Markdown with exactly these sections:
- `## Executive Summary`
- `## High-Confidence Actions`
- `## Watchlist`
- `## Rollback Plan`

## Guardrails
- Do not auto-apply to `openclix-config.json`.
- Preserve explicit confirmation gate.
- If sample size is insufficient, recommend hold and re-measure.
